import { CountryContinentColorPipe } from './country-continent-color.pipe';

describe('CountryContinentColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryContinentColorPipe();
    expect(pipe).toBeTruthy();
  });
});
